<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <!-- Estilo para el raised button -->
    <style name="RaisedButtonDark" parent="Theme.AppCompat.Light">
        <!-- cuando el boton este normal -->
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>

        <!-- cuando el boton este presionado -->
        <item name="colorControlHighlight">@color/colorPrimary</item>

        <!-- cambiar el color de la letra -->
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Estilo para los edit text con material design -->
    <style name="EditTextWhite" parent="TextAppearance.AppCompat">
        <!-- -->
        <item name="android:textColorHint">@color/editTextColorWhite</item>
        <item name="android:textSize">@dimen/size_EditText</item>
        <item name="android:textColorPrimary">@color/editTextColorWhite</item>
        <item name="colorAccent">@color/editTextColorWhite</item>
        <item name="colorControlNormal">@color/editTextColorWhite</item>
        <item name="colorControlActivated">@color/editTextColorWhite</item>
    </style>

    <!-- Estilo para los edit text con material design negros para el create account -->
    <style name="EditTextBlack" parent="Theme.AppCompat">
        <item name="android:textColorHint">@color/editTextColorBlack</item>
        <item name="android:textSize">@dimen/size_EditText</item>
        <item name="android:textColorPrimary">@color/editTextColorBlack</item>
        <item name="colorAccent">@color/editTextColorBlack</item>
        <item name="colorControlNormal">@color/editTextColorBlack</item>
        <item name="colorControlActivated">@color/editTextColorBlack</item>
    </style>
</resources>
